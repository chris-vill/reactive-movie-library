import React from 'react';
import classNames from 'classnames';
import classes from './SearchBar.sass';

const SearchBar = ({ extClass = "" }) => {

  return (
    <div>Search here</div>
  );
}

export default SearchBar;
